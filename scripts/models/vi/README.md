Put local Piper voice files here when copying the scripts folder to another project.

Each voice needs both files:

```text
<voice>.onnx
<voice>.onnx.json
```

Example:

```text
Duy Oryx.onnx
Duy Oryx.onnx.json
```
